# Server-side Google Maps API key
# Used for geocoding, route/travel-time computation, and venue search
# Must have Places API (New), Routes API enabled
MAPS_API_KEY=

# Client-side Google Maps API key
# Used only for rendering the interactive map in the browser
# Should be domain-restricted for security
VITE_GOOGLE_MAPS_API_KEY=
